# 📬 Agent Task Queue

> **🤖 Agent Routing:** This file acts as an async To-Do list for agents. If an agent is invoked via a schedule or background task, it should check this file, read the uncompleted tasks (`[ ]`), execute them, and mark them as `[x]` upon completion.

---

## Pending Tasks
- `[ ]` สรุปเนื้อหาของระบบ auth ทั้งหมดในโปรเจกต์ nextzy-gamification และ typesith ออกมาเปรียบเทียบกัน
- `[ ]` (Example) Generate INDEX.md based on current files.
- `[ ]` (Example) Run `/refactor` to deduplicate snippets.

## Completed Tasks
- `[x]` (Example) Ingest nextzy-gamification project.
